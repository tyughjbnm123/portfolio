{
  "name": "yichi-portfolio-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node preview.cjs",
    "build": "node build.cjs",
    "test": "node --test tests/*.test.cjs"
  }
}
